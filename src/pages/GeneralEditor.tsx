import { FC } from "react";
import Editor from "../components/editor/Editor";
import styled from "@emotion/styled";

const GeneralEditor: FC = () => {
  return (
    <div>
      <Editor />
    </div>
  );
};

export default styled(GeneralEditor)``;
